import React from "react";
import ReactDOM from "react-dom";
import {Router, Route, IndexRoute, browserHsitory} from "react-router";

const app = document.getElementById("app");

ReactDOM.render(
  <p> Hello World </p>,
  app
);
